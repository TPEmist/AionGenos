% Converted from d11_teacher_provenance.md — mechanical markdown->LaTeX (format only).
% Source draft H1 title: Teacher configuration provenance — D10 (L0a) vs L2 collects

\section*{Teacher configuration provenance --- D10 (L0a) vs L2 collects}

\tmlrnote{Run-provenance record for the cross-task comparison (L0a vs L2) that
the paper will report. The teacher config delta must not become
archaeology three months from now.}

\subsection*{The teacher config was NOT changed between D10 and L2 --- the task was}

\texttt{server/llama\_server\_teacher.sh} carried \texttt{--reasoning-budget 512}
unchanged from commit \texttt{a2aebb9} (2026-06-08) through the entire D10 /
D10-ext campaign (late June) and into the first L2 collect
(2026-07-14). Same binary, same model (gemma-4-31B-it Q4\_K\_M), same
mmproj, same ctx-size 16384, same \texttt{--parallel 1}.

\subsection*{The delta is task, not config --- and it surfaced a latent bug}

\begin{itemize}
\item \textbf{D10 (L0a-Left, single-arm):} inline Stage-4 recap generation
  succeeded almost completely --- across two 100-episode runs the
  collect logs show 0 and 1 ``empty content'' recap failures
  respectively (recaps\_d10 = 547 records total across 7 runs).
\item \textbf{L2 (dual-push) first collect:} inline recap failed 8/8 episodes,
  every one ``Empty content in VLM response''.
\end{itemize}

Same teacher config, opposite recap outcome. Root cause (confirmed
deterministically 2026-07-14): gemma-4's reasoning mode, enabled by
\texttt{--reasoning-budget 512}, can consume the entire 400-token output
budget on \texttt{reasoning\_content} and return empty \texttt{content}
(finish\_reason=length). The reflective recap prompt is the trigger,
and the L2 task inflates that prompt --- dual-arm state, more rounds,
longer round-history --- enough to push gemma into unbounded reasoning
where the terser L0a recap prompt did not. L0a's recap stayed under
budget by luck of prompt brevity, not by design.

\subsection*{Fix and its cross-task implication}

Teacher relaunched with \texttt{--reasoning off --reasoning-budget 0}
(commit \texttt{cabab3c}). Verified: the exact faithful L2 recap call that
returned empty now returns a real 469-char lesson in 7 s. Reasoning-off
is safe for L0a too (D10 succeeded on 512 precisely because it never
needed the reasoning tokens; removing them changes nothing there).

\textbf{Consequence for the paper's L0a-vs-L2 comparison:}
\begin{itemize}
\item D10 (L0a) recaps were generated \textbf{reasoning-on (budget 512)}.
\item L2 recaps are generated \textbf{reasoning-off (budget 0)}.
\item This is a teacher-config delta between the two tasks' \emph{recap
  buffers}, introduced by the fix. It does \textbf{not} affect the students
  (both trained from their respective buffers' text) but it is a real
  provenance difference to disclose if the L0a and L2 memory buffers
  are ever compared directly.
\item Stage-1 reasoning quality may differ subtly under reasoning-off vs
  reasoning-on. D10's Stage-1 ran reasoning-on; if a clean L0a-vs-L2
  comparison is needed, either (a) re-run L0a's recap buffer under
  reasoning-off for parity, or (b) report the delta explicitly. Cheap
  insurance recorded now rather than reconstructed later.
\end{itemize}

\subsection*{One-line summary for run tables}

\begin{itemize}
\item D10 / L0a teacher: gemma-4-31B Q4\_K\_M, ctx 16384, \textbf{reasoning-budget 512}.
\item L2 teacher (from 2026-07-14): same, \textbf{reasoning off / budget 0}.
\item Delta: reasoning off; forced by an L2-only empty-recap failure that
  L0a's shorter prompt had escaped by luck.
\end{itemize}
