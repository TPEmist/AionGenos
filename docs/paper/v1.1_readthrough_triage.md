# v1.1 second-pass read-through — triage (paper session, 2026-07-29)

Two parallel passes on the NEW v1.1 sections only (L2 chapter §4/5/6, D11
Discussion §5.3 contact-rich, abstract). (a) mechanical number/label/scope
reconciliation (paper session); (b) adversarial ICRA-Reviewer-2 pass
(agent). Every finding below is cross-checked against
`l2_confirmatory_report.md` §4 (binding may/may-NOT) and verified to exist
in-file before listing (claim-anchoring). Awaiting PI arbitration before
any fix lands.

## Mechanical pass (a) — result: CLEAN on numbers, 1 real cross-ref bug (fixed), 1 integration flag

- **Numbers:** 12 key values (SR 14/20, +6.0pp, z=1.129, p=0.259, CI
  [−4.5,+16.4], McNemar 9v3, R1 −17.1/−4.8/−17.2, ~72%, similarity
  0.900→0.870 / 0.847 vs 0.868, rescue 13.9 vs 21.6 z=−3.88, 5.5×/547,
  28.0%/42.9%, 56ep/511rounds) — all faithful to report. Zero drift.
  (Reviewer 2 independently confirmed same.)
- **Labels/scope:** exploratory/post-hoc labels all in place; no may-NOT
  violation on +6pp / causal-∝ / blanket-scope. Clean.
- **FIXED already (unambiguous single-point error):** L2 §6 bullet 5's
  bare `§5.3` cross-ref resolved to L2's OWN §5.3 (diagnostics) instead of
  the intended D11 contact section → disambiguated to
  `d11_discussion_draft.md §5.3` (commit b723b89).
- **T-INT (integration flag, NOT self-resolved):** two independent `§5`
  numberings collide — L2 chapter uses §5.1–5.4 AND D11 Discussion uses
  §5.1–5.4. Final numbering depends on whether the L2 chapter merges into
  §5 or becomes its own §6/§7. **PI/merge-time decision.**

## Adversarial pass (b) — Reviewer 2 findings, verified in-file

### FATAL

- **F1 — §4.4 states the effect "replicates" unqualified, reads as
  contradicting the n.s. verdict.** VERIFIED in-file (L134–136), exact
  quote confirmed; §5.1 L281 does say the opposite ("do **not** call +6pp
  a positive replication"). *Paper-session note on severity:* the sentence
  is the **pre-data locked claim wording** (Amendment 2, before results),
  living in Methods — not a false statement of the finding, but presented
  in unconditional present tense so it reads as realized. It also sits two
  lines under its own "if not significant → effect does not cross tasks"
  criterion, and the result was n.s. → genuine internal contradiction a
  reviewer will catch. **Fix = mark it as the pre-data conditional claim,
  not delete.** Proposed rewording (Reviewer 2's, lightly adjusted):
  *"The pre-data claim was locked a half-grade down — 'the
  identical-weights retrieval effect replicates on a second, harder task'
  — contingent on significance. The effect size did not reach it (§5.1);
  what replicates is the mechanism and direction only (§5.3 synthesis),
  not the effect size."* → **needs PI OK (touches a locked-claim wording).**

### PROSE-FIXABLE (paper session can apply on PI go)

- **P1 — §6 bullet 3 states the minimum-viable-competence "law"
  assertively without the paper-2-seed / exploratory hedge.** Report §4b
  requires it be paper-2 foreshadowing, one-liner, not a paper-1 result
  (mixes evidence tiers = "reviewer's easiest target"). Paper correctly
  kept it to one bullet and did NOT build out the three-faces structure
  (good); missing only the explicit hedge. Fix: add "(exploratory;
  developed as an a-priori hypothesis in the paper-2 pre-registration)".
  Aligns with the [[minimum-viable-competence-law]] memory (one sentence
  only).
- **P2 — §6 bullet 1 promotes the R1-restoration mechanism without the
  post-hoc caveat §5.3(b) carries.** VERIFIED: §6 bullet 1 (L370–376) says
  "restores the teacher's conditional correction (−17.2 ≈ −17.1)" with no
  post-hoc tag; the registered σ test was NULL (F=1.25), the mean
  comparison is post-hoc. Fix: carry "(post-hoc; the registered σ test was
  null)" into §6 or point to §5.3(b).
- **P3 — Abstract is L0a-only; a full L2 chapter with no abstract mention
  reads as burying an unfavorable (n.s.) second experiment.** VERIFIED
  abstract is single-arm/D11 only. Fix: ONE honest sentence. **TRAP
  (Reviewer 2 + mechanical agree):** the added sentence must NOT say
  "replicates"/"confirms" bare — same may-NOT #1 line as F1. Safe framing:
  "direction agrees, n.s./sub-MDE, mechanism replicates, size does not."
  → **candidate sentence below; needs PI OK (abstract is a headline
  surface).**
- **P4 — §6 folds a second task into a Discussion that frozen §5.1/§5.4
  still call "single-task."** Merge-time seam; touching frozen v1.0 prose.
  → **PI decision (v1.1 frozen-text touch):** reconcile "single-task" →
  "single-recipe, two tasks" at integration.
- **P5 — §5.3(b) singles out "training dose" as the candidate where at
  n=2 dose is confounded with task/buffer/contact.** Exploratory label +
  "no dose-response curve" present (covers it), but single-candidate
  framing is a soft target. Fix: one clause — "though at n=2 dose is
  confounded with task and buffer differences".

### RHETORIC (compliant, soft — optional)

- **Rh1 — §5.4 "42.9% gap-closed"** frames a null (CI incl. 0) as a
  positive fraction. Fully labelled post-hoc/exploratory + raw +6pp-n.s.
  alongside (compliant, may-NOT #2 clean). Optional: nothing required.
- **Rh2 — §5.4 opener "Raw pp understates…"** general-truth phrasing of a
  post-hoc device. Optional soften to "may understate".

### Reviewer 2 could NOT break (confirms our mechanical pass)

- Two-level seam (coverage=SIZE / start-distance=INCIDENCE): clean, no leak.
- Headroom confirmatory slip: none, label never drops.
- n=9 upgrade in Results §5: none (only the §6 carry-forward gap = P1/P2).
- Scope-word discipline: holds; §5.3 (contact) exemplary.
- Surface 6 (why-not-contact-rich): defense is complete for "why we
  stopped at reach", but the seam a reviewer probes is *"is the memory
  finding itself reach-specific?"* — the anatomy proves the INTERFACE
  can't reach contact-rich tasks, not that the marginal/conditional result
  would survive there. Not fatal (claim is scoped to reach), but
  **pre-arm the rebuttal**: "finding scoped to reach; contact-rich is a
  different control problem, out of scope" — a rebuttal note, not a text
  change.

## Candidate abstract L2 sentence (P3) — for PI approval, drafted to stay behind the n.s. line

> *On a second, harder task in the same primitive family, the effect
> direction agrees but does not reach significance (+6 pp, below the
> pre-registered MDE); the round-1 correction mechanism replicates while
> the effect size does not.*

(No "replicates"/"confirms" bare; "mechanism replicates" is scoped and
matches §5.3 synthesis + report §4-may. Raw +6pp + n.s. both present.)

## Recommended arbitration order
1. F1 — approve reworded pre-data-conditional framing (fatal, blocks
   internal consistency).
2. P3 — approve/edit the abstract sentence (headline surface).
3. P1, P2, P5 — prose caveats, paper session applies on a single go-ahead.
4. P4, T-INT — merge-time / frozen-text; defer to integration decision.
5. Rh1/Rh2, Surface-6 rebuttal note — optional / rebuttal file.
